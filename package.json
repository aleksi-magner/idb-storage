{
  "name": "purejs-idb",
  "author": "Aleksey Magner",
  "version": "1.2.4",
  "description": "Service for managing IndexedDB local storage.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git@github.com:aleksi-magner/purejs-idb.git"
  },
  "keywords": [
    "idb",
    "IndexedDB"
  ],
  "files": [
    "dist",
    "LICENSE",
    "README.md",
    "package.json"
  ],
  "main": "dist/index.js",
  "scripts": {
    "unit": "jest",
    "unit:dev": "jest --onlyChanged --watch",
    "build": "gulp",
    "deploy": "git push && git push --tags && npm publish",
    "patch": "npm run build && git add . && npm version patch --force -m 'Upgrade to %s' && npm run deploy",
    "minor": "npm run build && git add . && npm version minor --force -m 'Upgrade to %s' && npm run deploy",
    "major": "npm run build && git add . && npm version major --force -m 'Upgrade to %s' && npm run deploy"
  },
  "devDependencies": {
    "@babel/core": "^7.21.4",
    "@babel/preset-env": "^7.21.4",
    "@jest/globals": "^29.5.0",
    "babel-jest": "^29.5.0",
    "esm": "^3.2.25",
    "fake-indexeddb": "^4.0.1",
    "gulp": "^4.0.2",
    "gulp-babel": "^8.0.0",
    "gulp-plumber": "^1.2.1",
    "jest": "^29.5.0",
    "jest-environment-jsdom": "^29.5.0",
    "jsdom": "^21.1.1",
    "prettier": "^2.8.7"
  }
}
